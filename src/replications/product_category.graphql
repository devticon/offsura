query pullProductCategories  ($timestamp: timestamptz! $limit: Int! = 50) {
    product_categories  (
        order_by: {updatedAt: asc}
        limit: $limit
        where: {updatedAt: {_gt: $timestamp}}
    ){
        id
        name
        updatedAt
    }
}
